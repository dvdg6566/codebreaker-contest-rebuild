#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define MAXN (1000005)

#include "prisoner.h"

void prisoner(int N, int id) {
    for(ll i = 0;i < N;i++){
        openBox(i);
    }
}