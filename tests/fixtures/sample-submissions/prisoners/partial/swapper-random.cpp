#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define MAXN (1000005)
mt19937 rng1(500);
inline long long rand(ll x,ll y){ //random number between [x,y] inclusive
    return (rng1() % (y + 1 - x)) + x;
}
#include "swapper.h"

void swapper(int N, int boxes[]) {

}